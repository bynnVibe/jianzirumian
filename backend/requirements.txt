# 见字如养 - 依赖清单
# 安装: pip install -r requirements.txt

# ---- Web 框架 ----
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12

# ---- LangChain ----
langchain==0.3.1
langchain-community==0.3.1
langchain-text-splitters==0.3.0
# 显式钉住 langchain-core：否则 pip 会从最新版逐个回退试错（backtracking），
# 下载几十个元数据，网络不稳时直接导致构建失败；0.3.63 为与 requests==2.32.3 /
# pydantic==2.9.2 兼容的实测解析结果
langchain-core==0.3.63

# ---- 向量数据库 ----
# 注：如有编译问题，改为 `faiss-cpu>=1.8.0` (不带 exact pin)
# 或 pip install faiss-cpu --only-binary :all:
faiss-cpu>=1.8.0

# ---- Embedding ----
# Ollama 嵌入通过 langchain-community 的 OllamaEmbeddings
# API 嵌入通过 langchain-openai
langchain-openai==0.2.1

# ---- HTTP 请求 ----
httpx==0.27.2
requests==2.32.3

# ---- OCR (本地) ----
# PaddleOCR: pip install paddlepaddle paddleocr
# 注意: PaddleOCR 安装较复杂, 如不需要可跳过
# paddleocr>=2.8.0

# ---- 联网搜索 ----
duckduckgo-search==6.2.13

# ---- 工具 ----
python-dotenv==1.0.1
Pillow==10.4.0
pydantic==2.9.2
pydantic-settings==2.5.2

# ---- 重排序 (Reranker) ----
# 用于知识检索结果的相关度重排序
# 模型: BAAI/bge-reranker-v2-m3 (首次运行自动下载 ~2.2GB)
# 注：transformers 5.x 与 torch 2.2.x (macOS Intel 最后可用版) 不兼容, 需钉住以下版本组合
sentence-transformers==3.4.1
transformers==4.46.3

# ---- 文档解析 ----
python-docx>=1.1.0
# PyMuPDF 用于 PDF 文本/表格提取；pymupdf-layout 提供更准确的页面布局分析
# 并消除 "Consider using the pymupdf_layout package" 提示
PyMuPDF>=1.28.0
pymupdf-layout>=1.28.0

# ---- 缓存 ----
# Redis 客户端：知识库原始文件（图片/Word/PDF）缓存，加速检索来源跳转与预览；
# Redis 不可用时自动降级直读磁盘
redis>=5.0.0
